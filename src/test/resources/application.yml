spring:
  application:
    name: Project
    
  main:
    banner-mode: 'off'
  resources:
    cache:
      cachecontrol:
        max-age: 3600
        
  messages:
    basename: i18n/messages
             
server:
  compression:
    enabled: true
    
management:
  endpoints:
    web:
      exposure:
        include: '*'
    
info:
  app:
    name: Project
    description: Base Project description
    version: Version 1
    
app:
  management:
    userName: mgmt_user
    password: password